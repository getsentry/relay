use crate::protocol::{Mechanism, SystemSdkInfo};

pub(crate) fn normalize_mechanism_meta(
    mechanism: &mut Mechanism,
    sdk_info: Option<&SystemSdkInfo>,
) {
    // TODO
    let _mechanism = mechanism;
    let _sdk_info = sdk_info;
    unimplemented!();
}
