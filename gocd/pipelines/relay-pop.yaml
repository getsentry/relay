# More information on gocd-flavor YAML can be found here:
# - https://github.com/tomzo/gocd-yaml-config-plugin#pipeline
# - https://www.notion.so/sentry/GoCD-New-Service-Quickstart-6d8db7a6964049b3b0e78b8a4b52e25d
format_version: 10
pipelines:
    deploy-relay-pop:
        environment_variables:
            GCP_PROJECT: internal-sentry
            # Required for checkruns.
            GITHUB_TOKEN: "{{SECRET:[devinfra-github][token]}}"
        group: relay
        lock_behavior: unlockWhenFinished
        materials:
            relay_repo:
                git: git@github.com:getsentry/relay.git
                shallow_clone: true
                branch: master
                destination: relay
            # Relay PoPs can't be deployed until the relay deploy pipeline reaches
            # the mark-as-complete stage.
            pipeline-deploy-relay-completed:
                pipeline: deploy-relay
                stage: mark-as-complete
        stages:
            # checks isn't needed because it's implied by pipeline-deploy-relay-completed.
            - deploy:
                  approval:
                      type: manual
                  fetch_materials: true
                  jobs:
                      create_sentry_release:
                          environment_variables:
                              SENTRY_ORG: sentry
                              SENTRY_PROJECT: pop-relay
                              SENTRY_URL: "https://sentry.my.sentry.io/"
                              # Temporary; self-service encrypted secrets aren't implemented yet.
                              SENTRY_AUTH_TOKEN: "{{SECRET:[devinfra-temp][relay-sentry-auth-token]}}"
                          timeout: 1800 # 30 mins
                          elastic_profile_id: relay
                          tasks:
                              - script: |
                                    ./relay/scripts/create-sentry-release "${GO_REVISION_RELAY_REPO}"
                  # TODO: need to provision all these bastions
                      deploy-pop-europe-1:
                          timeout: 1800 # 30 mins
                          elastic_profile_id: relay-pop
                          environment_variables:
                              GKE_CLUSTER: pop-europe-1
                              GKE_REGION: europe-west3
                              GKE_CLUSTER_ZONE: b
                              GKE_BASTION_ZONE: b
                          tasks:
                              - script: |
                                    USE_GKE_GCLOUD_AUTH_PLUGIN=True \
                                        gcloud --project "$GCP_PROJECT" \
                                        container clusters get-credentials "$GKE_CLUSTER" \
                                        --zone "${GKE_REGION}-${GKE_CLUSTER_ZONE}" \
                                    && tmpdir=$(mktemp -d) \
                                    && ssh-keygen -q -t ed25519 -N '' -f "${tmpdir}/google_compute_engine" \
                                    && gcloud compute ssh "dicd-gkehop-${GKE_CLUSTER}" \
                                      --ssh-key-file="${tmpdir}/google_compute_engine" \
                                      --tunnel-through-iap \
                                      "--project=${GCP_PROJECT}" \
                                      "--zone=${GKE_REGION}-${GKE_BASTION_ZONE}" \
                                      -- -4 -L8888:127.0.0.1:8888 -N -q -f
                              - script: |
                                    /devinfra/scripts/k8s/k8sdeploy.py \
                                    --context="gke_${GCP_PROJECT}_${GKE_REGION}-${GKE_CLUSTER_ZONE}_${GKE_CLUSTER}" \
                                    --label-selector="service=relay-pop" \
                                    --image="us.gcr.io/sentryio/relay:${GO_REVISION_RELAY_REPO}" \
                                    --container-name="relay"
                      deploy-pop-us-1:
                          timeout: 1800 # 30 mins
                          elastic_profile_id: relay-pop
                          environment_variables:
                              GKE_CLUSTER: pop-us-1
                              GKE_REGION: us-central1
                              GKE_CLUSTER_ZONE: f
                              GKE_BASTION_ZONE: f
                          tasks:
                              - script: |
                                    USE_GKE_GCLOUD_AUTH_PLUGIN=True \
                                        gcloud --project "$GCP_PROJECT" \
                                        container clusters get-credentials "$GKE_CLUSTER" \
                                        --zone "${GKE_REGION}-${GKE_CLUSTER_ZONE}" \
                                    && tmpdir=$(mktemp -d) \
                                    && ssh-keygen -q -t ed25519 -N '' -f "${tmpdir}/google_compute_engine" \
                                    && gcloud compute ssh "dicd-gkehop-${GKE_CLUSTER}" \
                                      --ssh-key-file="${tmpdir}/google_compute_engine" \
                                      --tunnel-through-iap \
                                      "--project=${GCP_PROJECT}" \
                                      "--zone=${GKE_REGION}-${GKE_BASTION_ZONE}" \
                                      -- -4 -L8888:127.0.0.1:8888 -N -q -f
                              - script: |
                                    /devinfra/scripts/k8s/k8sdeploy.py \
                                    --context="gke_${GCP_PROJECT}_${GKE_REGION}-${GKE_CLUSTER_ZONE}_${GKE_CLUSTER}" \
                                    --label-selector="service=relay-pop" \
                                    --image="us.gcr.io/sentryio/relay:${GO_REVISION_RELAY_REPO}" \
                                    --container-name="relay"
                      deploy-pop-asia-1:
                          timeout: 1800 # 30 mins
                          elastic_profile_id: relay-pop
                          environment_variables:
                              GKE_CLUSTER: pop-asia-1
                              GKE_REGION: asia-northeast1
                              GKE_CLUSTER_ZONE: b
                              GKE_BASTION_ZONE: b
                          tasks:
                              - script: |
                                    USE_GKE_GCLOUD_AUTH_PLUGIN=True \
                                        gcloud --project "$GCP_PROJECT" \
                                        container clusters get-credentials "$GKE_CLUSTER" \
                                        --zone "${GKE_REGION}-${GKE_CLUSTER_ZONE}" \
                                    && tmpdir=$(mktemp -d) \
                                    && ssh-keygen -q -t ed25519 -N '' -f "${tmpdir}/google_compute_engine" \
                                    && gcloud compute ssh "dicd-gkehop-${GKE_CLUSTER}" \
                                      --ssh-key-file="${tmpdir}/google_compute_engine" \
                                      --tunnel-through-iap \
                                      "--project=${GCP_PROJECT}" \
                                      "--zone=${GKE_REGION}-${GKE_BASTION_ZONE}" \
                                      -- -4 -L8888:127.0.0.1:8888 -N -q -f
                              - script: |
                                    /devinfra/scripts/k8s/k8sdeploy.py \
                                    --context="gke_${GCP_PROJECT}_${GKE_REGION}-${GKE_CLUSTER_ZONE}_${GKE_CLUSTER}" \
                                    --label-selector="service=relay-pop" \
                                    --image="us.gcr.io/sentryio/relay:${GO_REVISION_RELAY_REPO}" \
                                    --container-name="relay"
                      deploy-pop-australia-1:
                          timeout: 1800 # 30 mins
                          elastic_profile_id: relay-pop
                          environment_variables:
                              GKE_CLUSTER: pop-australia-1
                              GKE_REGION: australia-southeast1
                              GKE_CLUSTER_ZONE: b
                              GKE_BASTION_ZONE: b
                          tasks:
                              - script: |
                                    USE_GKE_GCLOUD_AUTH_PLUGIN=True \
                                        gcloud --project "$GCP_PROJECT" \
                                        container clusters get-credentials "$GKE_CLUSTER" \
                                        --zone "${GKE_REGION}-${GKE_CLUSTER_ZONE}" \
                                    && tmpdir=$(mktemp -d) \
                                    && ssh-keygen -q -t ed25519 -N '' -f "${tmpdir}/google_compute_engine" \
                                    && gcloud compute ssh "dicd-gkehop-${GKE_CLUSTER}" \
                                      --ssh-key-file="${tmpdir}/google_compute_engine" \
                                      --tunnel-through-iap \
                                      "--project=${GCP_PROJECT}" \
                                      "--zone=${GKE_REGION}-${GKE_BASTION_ZONE}" \
                                      -- -4 -L8888:127.0.0.1:8888 -N -q -f
                              - script: |
                                    /devinfra/scripts/k8s/k8sdeploy.py \
                                    --context="gke_${GCP_PROJECT}_${GKE_REGION}-${GKE_CLUSTER_ZONE}_${GKE_CLUSTER}" \
                                    --label-selector="service=relay-pop" \
                                    --image="us.gcr.io/sentryio/relay:${GO_REVISION_RELAY_REPO}" \
                                    --container-name="relay"
